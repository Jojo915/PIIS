{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm: compile - backend",
            "type": "shell",
            "command": "npm run compile",
            "options": {
                "cwd": "${workspaceFolder}/backend"
            },
            "problemMatcher": "$tsc"
        }
    ]
}
